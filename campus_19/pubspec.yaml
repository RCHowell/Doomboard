name: campus_19
description: A collection of ehh boulder problems.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  test: ^1.14.2
  cupertino_icons: ^0.1.3
  path_provider: ^1.6.5
  path: ^1.6.4
  sqflite: ^1.3.0
  font_awesome_flutter: ^8.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/asset_db.sqlite3
